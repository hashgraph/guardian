Mechanically proven, still valid: compute → save → mint chain works; predictions matched live mints three times.
Methodologically corrected, in the CORRECTED file only: 0.89 removed, gate computed and fail-closed (now hardened against the tc=0 crash), baseline block aligned to Eq. 1, all stale metadata fixed.
