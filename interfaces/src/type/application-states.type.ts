/**
 * Application states
 */
export enum ApplicationStates {
    STARTED = 'STARTED',
    INITIALIZING = 'INITIALIZING',
    READY = 'READY',
    STOPPED = 'STOPPED',
    BAD_CONFIGURATION = 'BAD_CONFIGURATION'
}
