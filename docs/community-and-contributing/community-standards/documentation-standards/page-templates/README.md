# Page Templates

Paste the relevant template directly into GitBook. The page title goes in GitBook's title field — do not add a heading at the top of the page content.

Each template includes usage guidance above it. Read that before writing, then delete it before publishing.

For an explanation of when to use each type, see [..](../ "mention")
